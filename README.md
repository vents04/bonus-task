# Система за Управление на Банкови Сметки
## Bank Accounts Management System

Бонус задача за "Езици за Програмиране" - приложение за управление на банкови сметки с пълна функционалност за създаване, модификация и анализ на сметки.

Bonus task for "Programming Languages" - bank accounts management application with full functionality for creating, modifying, and analyzing accounts.

---

## 📁 Структура на Проекта / Project Structure

```
.
├── src/                    # Source files
│   ├── main.cpp
│   └── BankAccount.cpp
├── include/                # Header files
│   └── BankAccount.h
├── build/                  # Compiled object files (native, generated)
├── build_win/              # Compiled object files (Windows, generated)
├── Makefile               # Build configuration
└── bank_system           # Executable (generated)
```

---

## 🛠️ Изисквания / Requirements

**Prerequisites:** C++11 компилатор (g++ или clang++) и Make. На Windows: MinGW/MSYS2, Git Bash или WSL.

**Features:**
- Клас `BankAccount` с пълна капсулация (private променливи)
- Уникален код: буква + 5 цифри (напр. "A12345")
- Име на притежателя: указател към символен низ
- Динамични масиви за вноски и тегления
- Конструктори (по подразбиране, с параметри, копиращ), деструктор
- Предефинирани оператори `<<` и `>>`
- Четене и записване във файл
- Интерактивно конзолно меню

---

## 🔧 Компилация / Compilation

### Makefile (Препоръчително / Recommended):
```bash
make              # Компилира за нативна платформа
make windows      # Кръстосана компилация за Windows (.exe)
make all-platforms # Компилира за двете платформи
make run          # Компилира и стартира
make clean        # Премахва компилираните файлове
make help         # Показва всички команди
```

### Windows Cross-Compilation:
**macOS:** `brew install mingw-w64 && make windows`  
**Linux:** `sudo apt-get install mingw-w64 && make windows`

### Ръчна компилация / Manual:
```bash
g++ -std=c++11 -Wall -Wextra -pedantic -Iinclude \
    src/main.cpp src/BankAccount.cpp -o bank_system
```

---

## 🚀 Използване / Usage

```bash
./bank_system
```

**Основни функции / Main Features:**
1. Добави банкова сметка
2. Добави вноска към сметка
3. Добави теглене от сметка
4. Покажи всички сметки
5. Покажи детайли на сметка
6. Създай файл от сметки
7. Покажи притежатели с повече от една сметка (сортирани по азбучен ред)
8. Покажи разликите между вноски и тегления
9. Запиши сметки с равни вноски и тегления

---

## 📚 Клас BankAccount

**Характеристики:**
- Уникален код: Буква + 5 цифри
- Име на притежателя: Указател към символен низ
- Вноски/Тегления: Динамични масиви

**Методи:** Конструктори, деструктор, оператор за присвояване, `<<`/`>>`, `saveToFile()`/`loadFromFile()`, `addDeposit()`/`addWithdrawal()`, `getTotalDeposited()`/`getTotalWithdrawn()`/`getBalance()`, `hasEqualDepositsAndWithdrawals()`

---

## ✅ Валидация / Validation

- **Уникален код:** Точно 6 символа - буква + 5 цифри
- **Име на притежателя:** Не може да е празно
- **Суми:** Положителни числа (не могат да бъдат отрицателни)

---

## 📊 Файлове с Данни / Data Files

- `bank_accounts.dat` - Основен файл (автоматично записване/зареждане)
- `accounts.dat` - Създава се от опция 6
- `equal_accounts.dat` - Създава се от опция 9 (сметки с равни вноски и тегления)

---

## 🎯 Изисквания от Заданието / Task Requirements

✓ **1. Създава файл от обекти – банкови сметки** (Опция 6)  
✓ **2. Извежда имената на притежателите на повече от една банкови сметки, подредени по азбучен ред** (Опция 7)  
✓ **3. Извежда разликите от внесени и изтеглени суми** (Опция 8)  
✓ **4. Записва в нов файл информацията за сметките, за които внесените и изтеглени суми са еднакви** (Опция 9)

---

## 🔍 Технологии / Technologies

**C++11**, g++, Make, STL (vector, string, iostream, fstream, algorithm, map, set), exception handling, динамична памет (new/delete)

---

**Успех! / Good luck!** 🎯